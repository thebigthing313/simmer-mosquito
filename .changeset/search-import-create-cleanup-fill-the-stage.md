---
'@simmer-mosquito/web': patch
---

Changed: the search page, the weather import page, the assignments create page
and the record cleanup tools now span the width the loading skeleton reserves
for them, so none of them arrives narrower than the skeleton it replaces on a
wide screen. The search box, the import cards and the assignment form keep
their own widths; the frame around them is what widened.
