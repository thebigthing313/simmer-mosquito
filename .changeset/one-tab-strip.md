---
'@simmer-mosquito/web': patch
---

Changed: every tab strip in the app is now the same one, underlined tabs on no background, so moving between the habitat history, a trap's collections and the trap directory no longer looks like moving between three products. Fixed: the habitat history strip drew a vertical scrollbar of its own inside the tab row, and a strip narrow enough to scroll sideways cut its tabs in half.
