---
'@simmer-mosquito/web': minor
---

Changed: On the map pages the filters moved out of the column above the results and into a card beside them, opened from a filter control in the panel's header. The results rail now runs the full height under the place search and is 400px wide, which is roughly twice the records in view on a laptop screen. The filter control carries the number of filters that are set, so a narrowed list still says so while the card is away.
