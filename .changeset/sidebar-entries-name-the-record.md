---
'@simmer-mosquito/web': patch
---

Changed: Four sidebar entries that leaned on the group heading above them now
name the record they create. `Record Application` under Chemical reads `Record
Chemical Application`, `Record Release` under Biocontrol reads `Record
Biocontrol Action`, `Record Outreach` reads `Record Outreach Action`, and `New
Request` under Service Requests reads `New Service Request`. Read on its own,
in the command palette or on a narrow rail, each of the four named nothing in
particular. The same three buttons on the biocontrol, outreach and service
request maps read the same way now, the forms they open are headed and signed
the same, and the upcoming-page link to the chemical index reads `Chemical
Applications`.
