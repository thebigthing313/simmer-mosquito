---
'@simmer-mosquito/web': patch
---

Changed: three map pages are headed by the records they list, and the sidebar
group over each says the same words. `Source Reduction` is now `Source
Reductions`, `Biocontrol` is `Biocontrol Actions`, and `Outreach` is `Outreach
Actions`, on the page heading, on the back link from each one's create form,
in the sidebar group headings for source reduction and biocontrol, which had
been reading the old names, and on the links to those pages from the
Statistics pages that are not built yet. The Address Book keeps its name: an
address is an entry in it, and the book is the place you look one up.
