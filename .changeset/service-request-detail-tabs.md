---
'@simmer-mosquito/web': minor
---

Changed: the service request page splits into five tabs under its header, Details, Infrastructure, Surveillance, Control and Comments, and the column fits the viewport with each tab scrolling on its own. A family tab lists that family's nearby records nearest first and the map draws only those, so the family toggle chips are gone; Details and Comments draw the request and its radius alone. The active tab is in the address, so a refresh or a shared link lands on the same tab.
