---
'@simmer-mosquito/web': patch
---

Changed: a Habitat now carries the worm everywhere it is named. The mark was
three different glyphs depending on the screen, so the same record read as
waves on one page, a map pin on another and a network node on the habitats
explorer. `iconRegistry.entities.habitat` is the one answer and every surface
reads it.
