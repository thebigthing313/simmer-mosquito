---
'@simmer-mosquito/web': patch
---

Changed: a sample taken at no habitat and carrying no coordinates reads `Ad-hoc sample` on the sample detail, in the line under the title and in the Habitat row, where it used to read `Ad-hoc inspection`. The three larval surfaces that title a record by its habitat now read one label, so a sample, an inspection and the inspection explorer answer the same way.
