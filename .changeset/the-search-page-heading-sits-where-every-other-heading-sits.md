---
'@simmer-mosquito/web': patch
---

Changed: the search page's heading is the same heading every other framed page
draws, with the search glyph beside it. It had been a plain title a size below
the rest, sitting closer to the top of the page than the loading skeleton it
replaced, so the page shifted as it arrived. The search box stays under the
heading at the same width.
