---
'@simmer-mosquito/web': patch
---

Changed: the skeleton drawn while a page loads now spans the width the page
draws at. It used to sit in a 1200px column in the middle of the stage, so an
explorer, a record page or a split form arrived edge to edge with a sideways
jump at the moment it replaced the skeleton.
