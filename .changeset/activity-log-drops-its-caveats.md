---
'@simmer-mosquito/web': patch
---

Changed: the activity log no longer ends in a "What this log cannot show"
disclosure. The four caveats it listed described the records rather than the
log, so the panel repeated itself on every visit and sat under the entries a
person had come to read.
