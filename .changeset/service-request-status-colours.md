---
'@simmer-mosquito/web': patch
---

Changed: The service requests map draws open requests in red and closed ones in the resolved teal every other surface uses for finished work, with a key that narrows with the Status filter. The result rail carries the same colour as a dot, and rows drop the Open and Closed pills that repeated it.
