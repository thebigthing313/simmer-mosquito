---
'@simmer-mosquito/web': patch
---

Fixed: the other service requests around a request are no longer cut off the map by outreach actions in a dense radius. The page asks the nearby read for the record kinds it draws, so the 2000-row cap counts only those.
