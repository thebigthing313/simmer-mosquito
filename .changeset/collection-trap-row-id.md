---
'@simmer-mosquito/web': patch
---

Fixed: The Trap row on a collection's detail page named a trap with no code and no name by a short id taken from the collection rather than from the trap, so the row and the trap's own page disagreed.
