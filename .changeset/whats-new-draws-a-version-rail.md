---
'@simmer-mosquito/web': patch
---

Changed: on a wide screen each release on What's New is a row, with the version,
its date and the "You're on this version" badge in a rail down the left and the
entries beside them, so the page uses the width it has instead of leaving most of
it empty. On a narrow screen a release stacks as it did.
