export function PrimarySidebarActiveIndicator() {
	return (
		<div>
			An indicator for the active item in the primary sidebar. Along the right border of sidebar
			strip. Animates smoothly when active item changes (y translation)
		</div>
	);
}
