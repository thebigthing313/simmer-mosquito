/**
 * Build-time constants Vite substitutes via `define`. See vite.config.ts.
 *
 * These are literal replacements, not variables — nothing declared here exists
 * at runtime under its own name, so a value read from one of these is fixed at
 * the moment the bundle was built.
 */

/** The app's `package.json` version, moved only by `changeset version`. */
declare const __APP_VERSION__: string;
